import { Routes } from '@angular/router';
import { SignInComponent } from './pages/SignInPage/sign-in.component';

export const routes: Routes = [
    { path: 'sign-in', component: SignInComponent}
];

